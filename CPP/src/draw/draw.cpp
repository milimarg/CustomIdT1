/*
** EPITECH PROJECT, 2023
** doom
** File description:
** draw.c
*/

#include <SFML/Graphics.hpp>
#include <GL/gl.h>
#include "../../include/my.h"

void draw_point(int x, int y, sf::Color color, my_idt1 &world)
{
    //sf::VertexArray &a = world->points;
    //a.setPrimitiveType(sf::PrimitiveType::Points);
    //a.append(sf::Vertex(sf::Vector2f(x * world->map.pixel_scale, y * world->map.pixel_scale),
    //                    sf::Color(color.r, color.g, color.b)));

    //glColor3f(color.r, color.g, color.b);
    //glBegin(GL_POINTS);
    //glVertex2i(x * world->map.pixel_scale, y * world->map.pixel_scale);
    //glEnd();
}
